# 技术方案文档格式

> **按需加载**：本文件由 doc SKILL.md 在生成技术方案文档时 Read 加载，不随 SKILL.md 一起全量加载。

```markdown
# 技术方案

## 一、需求说明

### 项目背景
{从需求文档或 git commit message 提取}

### 目标
{需求目标}

### 收益
{预期收益}

### 功能点概述
{主要功能点列表}

### 相关链接
- Wiki/PRD：{链接}
- 需求ID：{OMJF-XXX}

---

## 二、详细设计

### 2.1 功能详细设计

#### 系统边界活动图
{描述系统边界和外部交互}

#### 时序图/流程图
{描述核心流程}

#### 系统内部交互
{描述内部模块交互}

#### 代码泳道图
{描述代码分层和职责}

---

### 2.2 交互接口设计

#### 2.2.1 REST 接口定义

**接口名称：{接口名称}**

**接口 URL：{HTTP 方法} {路径}**

**接口入参：**

| 字段 | 类型 | 必填 | 说明 | 样例 |
|------|------|------|------|------|
| xxx | String | 是 | xxx | "xxx" |

**接口出参：**

| 字段 | 类型 | 说明 | 样例 |
|------|------|------|------|
| code | Integer | 响应码 | 200 |
| message | String | 响应消息 | "success" |
| data | Object | 响应数据 | {...} |

---

#### 2.2.2 RPC 接口定义

**接口名称：{接口名称}**

**接口定义：{服务名}.{方法名}**

**接口入参：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| xxx | xxx | 是 | xxx |

**接口出参：**

| 字段 | 类型 | 说明 |
|------|------|------|
| xxx | xxx | xxx |

---

#### 2.2.3 MQ 定义

**消息主题：{Topic}**

**消费者组：{Consumer Group}**

**消息结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| xxx | xxx | xxx |

**消费逻辑：**
{描述消费逻辑}

---

#### 2.2.4 JOB 定义

**任务名称：{任务名称}**

**Cron 表达式：{cron}**

**任务说明：**
{描述任务逻辑}

---

### 2.3 数据模型设计

#### 2.3.1 MySQL 表

**表名：{table_name}**

**表说明：**
{表的业务含义}

**DDL 语句：**
```sql
CREATE TABLE `table_name` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  ...
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

**DML 语句：**
```sql
-- 新增数据
INSERT INTO table_name (...) VALUES (...);

-- 查询数据
SELECT * FROM table_name WHERE ...;
```

---

#### 2.3.2 Redis 数据

**Key：{key_pattern}**

**Key 说明：**
{key 的业务含义}

**Value 结构：**
| 字段 | 类型 | 说明 |
|------|------|------|
| xxx | xxx | xxx |

**样例：**
```
key: user:info:12345
value: {"name":"张三","age":25}
```

---

## 三、改动影响范围评估

### 3.1 代码改动范围

| 文件 | 改动类型 | 影响范围 |
|------|---------|---------|
| xxx.java | 新增 | 新增接口 |
| yyy.java | 修改 | 修改逻辑 |

### 3.2 接口改动影响

| 接口 | 改动类型 | 调用方 | 影响评估 |
|------|---------|--------|---------|
| /api/xxx | 新增 | 前端 | 需前端联调 |

### 3.3 数据改动影响

| 表/Key | 改动类型 | 影响评估 |
|--------|---------|---------|
| user_table | 新增字段 | 需执行 DDL |

### 3.4 依赖服务影响

| 服务 | 改动类型 | 影响评估 |
|------|---------|---------|
| user-service | 新增调用 | 需确认接口可用 |

---

## 四、稳定性设计

### 4.1 灰度降级设计

**灰度策略：**
{描述灰度发布策略}

**降级开关：**
| 开关名称 | 默认值 | 作用 |
|---------|--------|------|
| xxx_switch | false | xxx |

**降级逻辑：**
{描述降级后的处理逻辑}

---

### 4.2 监控告警设计

**监控指标：**
| 指标名称 | 说明 | 告警阈值 |
|---------|------|---------|
| xxx_qps | xxx 接口 QPS | > 1000 |

**告警配置：**
{描述告警规则和通知方式}

---

### 4.3 防资损设计

**资损风险点：**
| 风险点 | 防护措施 | 验证方式 |
|--------|---------|---------|
| xxx | xxx | xxx |

**对账机制：**
{描述对账逻辑}

---

## 五、其他 checkList

- [ ] 代码已自测
- [ ] 接口文档已更新
- [ ] 数据库变更已执行
- [ ] 配置项已添加
- [ ] 监控已配置
- [ ] 告警已配置
- [ ] 灰度方案已确认
- [ ] 回滚方案已准备
```
